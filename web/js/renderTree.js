/* renderTree.js — Renderer für view "tree" (verkettete Liste, Stack, Queue,
 * BST, Hashtabelle). Wird in Phase 6 implementiert. */
(function () {
  "use strict";
  window.RENDERERS = window.RENDERERS || {};
  // Noch kein Eintrag — der Player meldet sauber, falls ein tree-Trace geladen wird.
})();
