/* renderGraph.js — Renderer für view "graph" (BFS, DFS, Dijkstra).
 * Wird in Phase 7 implementiert. */
(function () {
  "use strict";
  window.RENDERERS = window.RENDERERS || {};
  // Noch kein Eintrag — der Player meldet sauber, falls ein graph-Trace geladen wird.
})();
